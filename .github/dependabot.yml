version: 2
updates:
  # This repository currently has no npm dependencies. Add an npm entry when
  # the first dependency and package-lock.json are committed together.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 0
    commit-message:
      prefix: "deps(ci)"
